# Xor-V2
A lightweight C command-line tool for XOR-encoding strings, IPs, and integer values into hex shellcode format. Useful for payload obfuscation, exploit development, and low-level networking projects.
